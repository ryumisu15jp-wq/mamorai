# MAMOR-AI 再開発 — レビューガイド

このプロトタイプを**手元で環境構築せずに確認する**ためのガイドです。実際の画面スクリーンショット（`review-screens/` 全11枚）と、確認ポイント・根拠・既知の限界をまとめています。

---

## 1. 何を作ったか（30秒サマリー）

警備業向け業務管理アプリ MAMOR-AI を、**React + Vite ＋ Tauri（PC専用デスクトップ）＋ Node/Express ＋ Supabase(Postgres)** で再開発した試作。TRAID パイプライン（TDD）で Sprint 1〜4 ＋ DB結合フェーズを実装。

- **共通入力ロジック層 `packages/input-core`（純粋TS・UI/DB非依存）**＝アプリの頭脳。日報・月報・リスク・シフト・拡張制約・通知・教育を全てここに集約。UIとサーバはこれを呼ぶだけ（将来のスマホ=Expo展開でロジック再利用可）。
- **10画面のデスクトップUI** ＋ **Claude鍵を隠す2つのサーバゲートウェイ**（リスク予測 / シフト最適化）。
- **実Postgresで DB結合・RLS・データ往復を実証**（テナント越境ゼロを実クエリで確認）。

## 2. 画面を見る（`review-screens/`）

| # | 画面 | 見どころ |
|---|---|---|
| 01 | 1分日報 | 右上に**タップ数 0/10**。meta/table/counter/check/gate の5セクション、±ボタン・トグル・前日プリフィル。自由入力(報告事項)は「任意」で必須にしない＝1分で提出できる設計。 |
| 02 | 月報 | 4指標(報告日数/対応件数/インシデント/承認率)・種別内訳＋前月比・日別推移バー・PDF/Excel出力(中間データ)。 |
| 03 | 日報一覧 | 状態/報告者/キーワードのフィルタ、承認/差し戻し、レコードの無い日は「未作成」。 |
| 04 | リスク | Claude予測を**サーバ経由**で取得→リスク度降順、High/Mid/Lowバッジ、種別/ポジション絞り込み。 |
| 05 | シフト | スタッフ×日付の勤務表、区分別集計。手動編集は既存shiftsを壊さず override 保存。 |
| 06 | 配置表 | 日次のポジション別配置、**欠員を赤で明示**。 |
| **07** | **制約** | ★ご要望の中核。**国(労基)/保険/会社/勤務条件/その他 を自由に追加**、hard/soft、根拠(例 労働基準法第32条)、weight、ON/OFF。 |
| **08** | **AIシフト** | ★「**自動確定は行いません**」。自然言語→Claude構造化→制約反映→下案生成→**管制員が確認して初めて確定(HITL)**。 |
| 09 | 通知 | 種別(重要/業務指示/本部)×対象(全員/現場/勤務種別/役割)で配信対象プレビュー・未確認件数。 |
| 10 | 教育 | 資格の有効/更新間近/期限切れバッジ、研修進捗バー(例 32/45h)。 |
| 11 | テンプレ設定 | 現場ごとにセクションON/OFF→入力フォームに即反映（過去集計は壊さない）。 |

## 3. 品質の根拠（確認できる証拠）

| 項目 | 結果 | 証拠ファイル |
|---|---|---|
| 共通ロジック層テスト | **264 / 264 GREEN**（lines99%/branches91%/funcs100%） | `packages/input-core/src/__tests__/` |
| サーバ（実DB統合含む） | **27 / 27 GREEN** | `server/src/db/*.integration.test.ts` |
| デスクトップ(Tauri抽象) | 4 / 4 GREEN | `apps/desktop/src/lib/tauriBridge.test.ts` |
| DBマイグレーション | 0000/0100/0102/0103/0200/0201/0202 を実DBへ**0エラー適用・破壊的変更ゼロ** | `reports/db-apply-log.txt` |
| RLS テナント越境 | 担当外現場データ**全18表で0件**・越境INSERT拒否・AI経路書込はapp_serviceのみ | `reports/rls-hardening-verification.txt` |
| 各Sprint 品質判定 | S1/S2/S4 PASS、**S3・DB結合は FAIL→是正→PASS** | `reports/sprint{1-4}-evaluation.md`, `reports/production-phase-evaluation.md` |

**特筆**: Sprint3 で「AIが知らない安全ルールを"問題なし"と誤判定（＝違法配置リスク）」を、DB結合で「他現場の個人情報が読めてしまう越境」を、いずれも**実検証で発見して是正**しました。フェイルセーフ設計（評価できない hard 制約は不合格扱い）とテナント分離が効いています。

## 4. 手元で動かす場合（任意）

`README.md` に全手順。要点だけ:
```bash
pnpm install
pnpm --filter @mamorai/input-core exec vitest run --coverage   # 264件
pnpm --filter @mamorai/desktop exec vite preview                # 画面を確認
# DB結合の再現(Postgres必要)は README「本採用フェーズ(DB結合の実証)」参照
```

## 5. 既知の限界（正直に）

- **実データは推定**: 既存25テーブルの実DDLが無いため `0000_base_assumed.sql` は推定スキーマ。実DDL提供時に差し替え（隔離点は `app_user_site_ids()` / `app_is_service()` の2関数と `0000` のみ）。
- 実Claude予測エンジン結線・OR-Tools本実装・Tauri実ビルド(WebView2)・E2E・実Supabase接続は本番結線フェーズ。
- UIはデモデータ表示（保存はサーバAPI経由の設計、画面は未接続プレビュー）。

## 6. ご判断いただきたいこと

- **(A)** 実25テーブルDDL／実Supabase認証を提供 → 本番結線（`0000`置換・`auth.uid()`結線・実接続）。
- **(B)** 特定画面や仕様の**修正・追加要望**（このプロトタイプに反映）。
- **(C)** 現状で一旦クローズ。

気になった画面番号や機能を指定いただければ、その部分を深掘り／修正します。
