# シフト作成ルール（会社設定・人事総務部）

現時点で義務ではないが、各現場の人員補充の参考にする社内ルール。
MAMOR-AI では「拡張制約フレームワーク（会社カテゴリ）」として登録し、
**労務アラート**で自動点検する。会社ごとに追加・調整可能（データ駆動＝行を足すだけ）。

| # | ルール | 制約 kind | パラメータ | 種別 |
|---|---|---|---|---|
| ① | 当務(22〜25h)の後は連続シフトを入れず、次の勤務開始まで最低1日空ける | `rest_day_after_long_shift` | `minHours:22, restDays:1` | hard |
| ② | 夜勤の後に連続して勤務しない（翌日は休み） | `no_work_after_night` | `nightTypes:['夜勤']` | hard |
| ③ | 勤務が過密にならないよう休日配置に配慮 | `min_days_off_per_week` | `days:1` | soft |
| ④ | 退勤〜翌始業まで9時間以上のインターバルを確保 | `min_rest_hours` | `hours:9` | hard |

## 実装メモ
- 勤務区分に **当務（25h想定：08:00→翌09:00）** を追加。`shiftDurationHours` で22h以上を当務とみなす（①のトリガ）。
- ①②④は翌日/インターバルの実データ（勤務時刻）で判定。③は完全な週（7日揃い）で休日が規定未満のとき注意。
- これらは `evaluateLaborAlerts()`（純粋関数）で評価し、「労務」タブに表示。判定ロジックはUIから分離。
- 会社ごとに値（時間・日数・対象区分）を変えるだけで運用差を吸収。将来、現場別に上書きも可能。

## 今後
- 従業員スマホアプリからのシフト/休暇申請は「PCコンソール整備後」に着手（人事総務部方針）。
- 現場別のルール上書き、しきい値の画面編集（制約タブ）を拡充予定。
