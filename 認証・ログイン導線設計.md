# MAMOR-AI 認証・ログイン導線設計（3系統）

ドメイン基点は env 化（当面 `mamorai.tryangrow.com`、後日独自ドメインへ張替のみ）。
サービスページ／LP は静的、アプリ本体（3系統ログイン）は SPA + APIゲートウェイ（Claude鍵秘匿）。

| パス | 対象 | ログイン方式（推奨） | セッション範囲 | 想定端末 |
|---|---|---|---|---|
| `/` | サービスページ | 認証なし（公開） | — | 誰でも |
| `/lp` | LP（集客） | 認証なし（公開） | — | 誰でも |
| `/co/` | 会社（本社・権限者） | **メール＋パスワード（＋任意MFA/TOTP）** | 会社スコープ（自社の全現場） | 事務所PC |
| `/s/` | 施設（現場） | **会社識別コード＋施設コード＋現場PIN** | 単一現場スコープ | 現場の共有端末 |
| `/tradmin/` | 運営（TRYANGROW） | **メール＋パスワード＋MFA必須** | プラットフォーム全体 | 社内・限定 |

## `/co/` 会社ページ（本社・権限者）
- **方式**: メールアドレス＋パスワード。Supabase Auth をIdPに使用。権限者は TOTP MFA を任意で有効化可能。
- **役割(role)**: `company_admin`(本社管理者) / `company_manager`(権限者)。ロールで機能可視範囲を制御。
- **データ範囲**: 自社に属する全現場（RLSは会社IDでスコープ）。現場横断の集計・配置・AIシフト・通知・教育管理はここが起点。
- **理由**: 管理者は個人が特定される正規アカウントが必要（監査・権限分掌）。パスワードリセット/招待制でアカウント発行。

## `/s/` 施設（現場）ページ
- **方式**: 会社識別コード（company code）＋施設（現場）コード（site code）＋現場PIN。
  - 会社識別コード: 会社ごとの短い固定コード（例 `TRA-8821`）。
  - 施設コード: 現場ごとの固定コード（例 `LALA-01`）。
  - 現場PIN: 現場端末で共有する短い暗証番号（定期ローテーション推奨）。
- **セッション範囲**: 入力した1現場のみ（RLSで site_id スコープ、他現場は0件）。
- **理由**: 現場は共有端末・交代制でメールアカウント運用が現実的でない。低摩擦・貼り紙運用に耐える識別＋PIN方式。個人特定は日報の「対応者」欄で担保。
- **強化オプション**（本結線時）: 端末バインド（初回に端末トークン発行→以降PINのみ）、PIN試行回数ロック、現場端末IP/位置の許可リスト。

## `/tradmin/` 運営（TRYANGROW）ページ
- **方式(現行推奨)**: メールアドレス＋パスワード＋**MFA必須**。`platform_admin` ロール。顧客認証とは**別レルム**で分離（顧客DB侵害が運営権限に波及しない）。
- **今後を見据えた別ログインシステム（roadmap）**: 従業員増を見据え、**社内IdP（Google Workspace SSO / SAML/OIDC）** で一元管理へ移行。入退社に応じたプロビジョニング/デプロビジョニング、最小権限（テナント閲覧・サポート代理ログインは監査ログ必須）。
- **理由**: 運営権限は最上位。多要素・別レルム・監査で保護。将来はSSOで従業員ライフサイクル管理と分離。

## 実装状況（本スプリント）
- 3系統のログイン画面（`CompanyLogin` / `SiteLogin` / `TradminLogin`）と、`window.location.pathname` で振り分ける `EntryRouter` を React で scaffold（同一Reactコードを Web/Tauri 両出し）。
- 認証の実結線（Supabase Auth / MFA / セッション）は本結線フェーズ。現状はUI導線とバリデーション形をデモ表示。
- サービスページ(`/`)・LP(`/lp`)は静的HTMLで作成。
